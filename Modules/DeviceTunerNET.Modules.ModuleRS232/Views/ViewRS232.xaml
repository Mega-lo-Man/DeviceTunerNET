<UserControl x:Class="DeviceTunerNET.Modules.ModuleRS232.Views.ViewRS232"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:DeviceTunerNET.Modules.ModuleRS232.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:validators="clr-namespace:DeviceTunerNET.Modules.ModuleRS232.Validators"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True" >
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.5*"/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Label Content="Нажми кнопку" Grid.Row="0" Grid.Column="0" />
        <TextBox Grid.Row="1" Grid.Column="0">
            <TextBox.Text>
                <Binding Path="StartAddressTextBox" NotifyOnValidationError="True" UpdateSourceTrigger="PropertyChanged">
                    <Binding.ValidationRules>
                        
                    </Binding.ValidationRules>
                </Binding>
            </TextBox.Text>
        </TextBox> 
        <ComboBox ItemsSource="{Binding AvailableComPorts}" SelectedItem="{Binding CurrentRS485Port}" Grid.Column="1" HorizontalAlignment="Left" VerticalAlignment="Center" Width="65" Grid.ColumnSpan="2" Margin="0,2,-15,2">
        </ComboBox>
        <Button Content="Сдвинуть адреса" Grid.Row="0" Grid.Column="2" Command="{Binding ShiftAddressesCommand}" />
        <Button Content="Сканировать сеть" Grid.Row="0" Grid.Column="2" Command="{Binding ScanNetworkCommand}" />
    </Grid>
</UserControl>
